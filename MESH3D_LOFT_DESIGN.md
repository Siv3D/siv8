# Mesh3D Loft: 任意の配置を持つ断面の接続案

状態: 設計検討。以下の新しい型・overload・契約は未実装であり、公開ヘッダの仕様を変更するものではない。

## 進める理由

Assembly の部品・共有形状・材質・階層複製・位置合わせ・OBJ / MTL 出力を調べた範囲では、Loft の評価を先送りする必要のある取りこぼしは見つからなかった。面ごとの材質、別 Assembly への取り込み、自動プレビューと診断は独立した拡張として `TODO.md` に残す。

次は、断面の形と向きを両方変えながらつなぐ、曲がった外装・ノズル・取っ手などを評価する。現行 Loft は同じ頂点数の輪郭を昇順の Y 座標で接続する。断面の移動や変形はできるが、断面平面自体を途中で傾けられない。Sweep は経路から姿勢を作り、一定輪郭を scale / twist で変える用途に適している。Loft は各断面の輪郭と配置を利用者が明示する用途を受け持つ。

複数のループを対応づけて面で接続する考え方は、Blender の [Bridge Edge Loops](https://docs.blender.org/manual/en/5.3/modeling/meshes/editing/edge/bridge_edge_loops.html) と共通する。同ツールは頂点対応のずらしや中間ループの補間も扱うが、それらを一度に導入する必要はない。本案はサンプリング済みの平面輪郭を三角形で接続し、CAD の曲面補間や連続性を保証する Loft とは区別する。

## 推奨する入力の形

断面の輪郭と配置を 1 要素にまとめ、`sections` と `heights` の並行配列を解消する。新しい公開概念は `LoftSection` と `LoftOptions` の 2 つとし、配置と端面指定には既存の型を使う。

```cpp
// 設計案: 未実装
struct LoftSection
{
    std::span<const Vec2> points;
    Mesh3DPlacement frame{ Mat4x4::Identity() };
};

struct LoftOptions
{
    Mesh3DEndCaps endCaps = Mesh3DEndCaps::Both;
    double smoothingAngle = 0.0;
    Vec2 uvScale{ 1.0, 1.0 };
    Vec2 uvOffset{ 0.0, 0.0 };
};

// canonical な入力は span。配置ありも同じ destination-writing generator を使う。
Mesh3D Mesh3D::Loft(std::span<const LoftSection> sections,
                   const LoftOptions& options = {});
Mesh3DAddResult Mesh3DBuilder::addLoft(std::span<const LoftSection> sections,
                                     const LoftOptions& options = {});
Mesh3DAddResult Mesh3DBuilder::addLoft(std::span<const LoftSection> sections,
                                     const Mesh3DPlacement& placement,
                                     const LoftOptions& options = {});
```

短い手書き入力用の initializer-list wrapper は factory と配置なし builder に 1 つずつ加える。配置ありでは `std::array<LoftSection, N>` などを渡す。既存の Array 専用・断面数と頂点数をテンプレート化した overload は、実験段階の破壊的変更として置き換える方向を推す。これなら factory の 3 宣言と builder の 6 宣言を、合計 5 宣言へ整理できる。移行前にリポジトリ内の全呼び出しを確認する。

`LoftSection` は輪郭を所有しない。輪郭の配列は呼び出し終了まで生存させる。同じ輪郭の span を複数の断面で共有でき、同じ `Array<LoftSection>` も再利用できる。借用 span を一時的な輪郭配列から作って保存する例は掲載しない。

`Mesh3DPlacement` は現在 `Mesh3DBuilder.hpp` に定義されている。`Mesh3D.hpp` から利用するには、この既存型を独立したヘッダへ移す。新しい Frame 型や Anchor 型は追加しない。移動時には公開 umbrella と macOS / Windows のプロジェクトを更新する。

## 座標と断面の向き

現行の `Vec2(x, y) → Vec3(x, 0, -y)` を維持し、その点に `frame` を適用する。単位フレームの輪郭平面は XZ、正方向は +Y である。全体配置はその後に適用する。旧 `heights[i]` は各断面の `frame = Vec3{ 0, heights[i], 0 }` に対応する。

frame を任意のアフィン変換にする案を推す。非一様スケールを frame に持たせれば、共有する輪郭を幅違い・厚み違いで利用できる。平面性も保たれる。保持精度は既存の Mesh3DPlacement と同じ float であり、double の位置を保持する新しい座標系型を導入したと誤解させない。

初期案では、断面 frame は有限・非特異で、線形部分の determinant が正のものに限定する。局所的な鏡映で輪郭の表裏を逆転させず、形状全体の鏡映は生成後の Assembly 配置で行う。端面の正方向は単純に変換した +Y ではなく、変換した輪郭平面の法線（逆転置または平面の基底の外積）から求める。これはせん断を許す場合に必要となる。

任意フレームでは「高さの昇順」に代わる局所的な方向規約が必要である。初期案では、隣接するフレーム原点の変位が、両端の断面正方向にそれぞれ正の投影を持つことを要求する。これにより進行方向と端面の向きが整合する。曲がる場合は必要な中間断面を与える。原点が一致する断面同士の接続や、1 区間で進行方向を反転する入力は初期対象にしない。

この条件は側面の交差や局所的な折り返しを完全に防ぐものではない。非自己交差の閉じた立体であることを、条件に合格しただけで保証しない。方向条件が実用上狭すぎるかは、後述の題材で検証してから公開契約を確定する。

## 頂点対応・端面・法線・UV

- **対応:** 全断面は同じ 3 個以上の頂点数を持ち、同じ添字同士を接続する。輪郭の正の符号付き面積、始点を末尾に重複させない規約は維持する。最も近い頂点への自動対応や、輪郭の自動反転を行わない。非平面となる側面の四角形は、現行と同じ対角線で三角形化する。
- **端面:** `Mesh3DEndCaps` の None / Start / End / Both をそのまま使う。始端は断面の負方向、終端は正方向。要求された端面だけをローカル 2D 輪郭で三角形化し、その後 frame へ配置する。端面なしでは、そのための Polygon と triangulation を作らない。断面列の閉路化、輪郭の穴、点への収束は初期対象にしない。
- **法線:** 既定の `smoothingAngle = 0` は現行同様、輪郭方向をハードエッジ、断面列方向をスムーズにする。正の角度で隣接する輪郭側面間の補間を指定する方向を推す。端面は側面と分離する。閾値の評価を各断面で行うか、同じ輪郭頂点の全断面で共有するかは試作で決める。入力の局所的な変更で頂点分割が変わる可能性を契約に含める。
- **側面 UV:** U は frame 適用後の最初の輪郭の周長を 0～1 に正規化し、後続断面にも同じ頂点対応を使う。V はフレーム原点の折れ線に沿う累積距離とする。単なる Y 座標の差や任意の三角形辺の距離にはしない。高さだけを使う旧入力では現行の値に一致する。
- **UV の限界:** 原点は利用者が指定する基準線であり、輪郭の重心から自動推定しない。同じ形状でも原点の取り方を変えると V が変わり得る。実際の側面上で一様な距離密度になる保証はない。独立した経路長・頂点別 UV の入力は、具体的な不足が出るまで増やさない。
- **端面 UV と接線:** 端面 UV はローカル輪郭の境界から現行と同じ規約で作る。非一様スケール時の密度の差は許容する。法線・接線は生成される面と UV に対応させ、せん断、負の UV scale、U seam、平滑化した境界も検証する。

## 輪郭サンプリングは別の責務

Loft に自動リサンプリングや曲線補間を入れると、頂点の対応・角の位置・UV seam の位置を暗黙に変えてしまう。初期実装は渡された輪郭だけを接続し、中間断面もレシピが作る。

既存の `Circle::asPolygon()` / `pointAtLength()`、`Bezier2/3::pointAt()`、`LineString::computeLength()` を入力準備に利用できる。単純な等弧長サンプリングだけでは「四角形の角を円形のどの位置へ対応させるか」は決まらない。追加の点列 utility が必要になった場合は、始点・巻き順・角などの基準点を明示して保持する方式を 2D geometry 側で検討する。LoftSection に円・楕円・角丸矩形などの専用 constructor を増やさない。

## 実装の足場

現行 `Mesh3DLoft.cpp` は検証・端面準備・個数計算・書き込みを 1 関数に持つ。最初に内部の準備処理と出力処理を分け、既存の出力と失敗時非変更を維持する。その上で、同じ出力処理へ frame 適用後の断面点列を渡せるようにする。

- 準備側は参照する断面、変換済み点、原点の累積距離、要求された端面の triangulation をまとめる。既存の ring / cap validation と CheckedAdd / CheckedMultiply を再利用する。
- 新しい一般形では、float にした最終座標で消える辺、側面の縮退三角形、法線・接線が定義できない局所配置を、destination 変更前に検出する。側面全体の自己交差検査とは分ける。検証コストと許容範囲は試作で測り、既存の頻繁に呼ばれる経路へ例外入力対策の分岐を追加する場合はリポジトリ規則に従って事前に承認を得る。
- 書き込み側は確定した個数だけ destination を拡張し、範囲へ直接出力する。断面ごとの一時 Mesh3D や、factory での別実装を作らない。
- 高さだけの生成では不要だった各点へのアフィン変換や側面検証が増える。無条件に旧処理を重くせず、共通の書き込み処理へ接続できる高速経路の要否を測る。高速経路が必要でも、公開 overload を戻すこととは分けて考える。
- Sweep の `PathFrame` / `PathData` は現時点では内部表現である。Loft の導入だけを理由に公開せず、共通化は輪郭接続や法線計算など、実際に同じになる処理に限定する。
- 新しい一般形が安定してから、旧 overload とその重複 Doxygen を削除し、テスト・manual の呼び出しを移行する。移行中の内部比較用経路と、長期に残す公開 API を混同しない。

## 評価題材と完了条件

1. **傾いた断面を持つノズル:** 長方形から角丸断面へ変化しながら曲がる。単なる Sweep の scale / twist では表現できない輪郭変化を含め、取付側は端面なし、出口は開口とする。まず外側の面だけで接続を評価し、端面なし指定だけで内壁や肉厚も生成できるとは扱わない。
2. **曲面主体の外装:** 断面数・輪郭分割数を変えても、既存の Assembly の MeshID を `setMesh()` で維持し、部品 ID・材質・配置が変わらないことを確認する。輪郭に沿う smooth / hard の差を評価する。
3. **取り付けと鏡映:** 端部の frame をレシピから返し、Align で別部品を接続する。同じ生成物を cloneSubtree と鏡映配置で使い、法線と winding を確認する。
4. **基礎検証:** 高さだけの既存例、最小の 2 断面×3 頂点、凹輪郭、全端面設定、非一様スケール・せん断、UV 反転、局所的な折り返し・縮退、無効な frame、頂点数上限、失敗時の destination 非変更を検証する。側面の自己交差が検査対象外であることも説明する。

最初の試作で判断するのは、フレームの方向条件、輪郭の平滑化の基準、旧経路に対する生成コストの 3 点である。既存契約との両立に過度な複雑さが必要な場合は、実装を増やす前に契約を再検討する。

## 現行コードの参照先

- 公開 API と旧座標規約: `Siv3D/include/Siv3D/Mesh3D.hpp` の Loft、`Mesh3DBuilder.hpp` の addLoft
- 生成と端面処理: `Siv3D/src/Siv3D/Mesh3D/Mesh3DLoft.cpp`
- 既存の回帰検証: `Test/Test_Mesh3DLoft.cpp`、`Test/Test_Mesh3DBuilder.cpp`、`Test/Test_Mesh3DCapTriangulation.cpp`
- 経路からのフレーム生成との比較: `Siv3D/src/Siv3D/Mesh3D/Mesh3DTube.cpp`

この文書は設計の判断材料を保持する。実装完了後は確定した契約を公開ヘッダへ、未解決事項だけを TODO へ移し、重複する設計記述を整理する。
